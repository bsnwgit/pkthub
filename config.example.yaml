host: "0.0.0.0"
port: 8760
# Installs on HTTP by default. Set to true and add cert paths (uploaded via
# Settings > SSL) only after enabling HTTPS in the systemd unit's ExecStart.
https: false
ssl_certfile: ""
ssl_keyfile: ""

# Secret key for JWT signing — generate with: openssl rand -hex 32
jwt_secret: "CHANGE_ME_generate_with_openssl_rand_hex_32"
jwt_algorithm: "HS256"
jwt_expire_minutes: 60

# Credential encryption key (generate with the Fernet command below) — used
# to encrypt stored secrets (user API keys) at rest.
# python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
credential_key: "CHANGE_ME_generate_with_fernet_generate_key"

# Initial admin user (created on first boot if no users exist)
initial_admin_username: "admin"
initial_admin_password: "CHANGE_ME"
initial_admin_email: "admin@example.com"

# Okta/OIDC (optional — leave blank to disable)
okta_domain: ""
okta_client_id: ""
okta_client_secret: ""

# SQLite database path (defaults to <install_dir>/pkthub.db if omitted)
db_path: ""

# Health polling interval (seconds)
health_poll_interval: 30

# Audit log retention (days)
audit_retention_days: 90

# Trusted CIDRs (empty = allow all)
trusted_cidrs: []
